# Copies Zathura text to the system clipboard
set selection-clipboard clipboard

#When zooming, zoom in on the centre by default
set zoom-center true

#Scrolling by half or full pages stops at page boundaries
set scroll-page-aware true
